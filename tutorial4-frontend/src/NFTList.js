function NFTList(walletConnected) {
  console.log(walletConnected);

  //array.forEach(element => {});

  return (
    <></>
    /*<ul>
      {recommendations.map((rec, index) => (
        <li key={index}>{rec}</li>
      ))}
    </ul>
    */
  );
}

export default NFTList;
